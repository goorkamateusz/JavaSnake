public class Gameplay extends GameBase {

    @Override
    protected void awakeScene() {
        initialize(new TestSqrt());
        initialize(new TestSqrt());
        initialize(new TestSqrt());
        initialize(new TestSqrt());
        initialize(new TestSqrt());
        initialize(new TestSqrt());
        initialize(new TestSqrt());
        initialize(new TestSqrt());
    }
}
